---
apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    name: orders
  namespace: toy-shop
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 7001
  selector:
    name: orders
